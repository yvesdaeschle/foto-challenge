## Agents Instructions

- **At the end of every response**, you must use the `vscode_askQuestions` tool to ask the user whether the task is complete or if there are further instructions. This applies to every prompt without exception but not within a subagent.